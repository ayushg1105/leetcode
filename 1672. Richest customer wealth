class Solution(object):
    def maximumWealth(self, accounts):
        """
        :type accounts: List[List[int]]
        :rtype: int
        """
        m = len(accounts)
        w = []
        for i in range (m):
            c = sum(accounts[i])
            w.append(c)
        return max(w)        
